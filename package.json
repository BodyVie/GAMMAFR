{
  "name": "gammafr",
  "version": "1.0.0",
  "private": true,
  "description": "Site statique + Cloudflare Worker — localisation française du modpack S.T.A.L.K.E.R. G.A.M.M.A.",
  "scripts": {
    "lint": "for f in js/*.js tests/*.js; do node --check \"$f\" || exit 1; done; node --check --input-type=module < worker.js",
    "test": "node --test"
  }
}
